#include <iostream>
using namespace std;
int main() {
    int a, b, c;
    cin >> a >> b >> c;
    if (a >= 102 && b >= 102 && c >= 102)
        cout << "YES";
    else
        cout << "NO";
    return 0;
}